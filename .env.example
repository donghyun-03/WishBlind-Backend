# 이 파일을 복사해서 .env 로 만들고 실제 값을 채우세요.
# .env는 절대 git에 커밋 금지 (.gitignore에 이미 포함).

# 앱이 접속할 DB 이름 / 계정
DB_NAME=wishblind
DB_USERNAME=root
DB_PASSWORD=changeme

# docker-compose의 mysql 컨테이너 root 비밀번호 (보통 DB_PASSWORD와 동일하게)
DB_ROOT_PASSWORD=changeme
